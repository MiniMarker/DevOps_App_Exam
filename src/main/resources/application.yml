spring:
  datasource:
    url: ${JDBC_DATABASE_URL}
    username: ${JDBC_DATABASE_USERNAME}
    password: ${JDBC_DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create-drop

graphite:
  host: ${GRAPHITE_HOST}
  apiKey: ${HOSTEDGRAPHITE_APIKEY}

server:
  port: ${PORT}

